#include <stdio.h>

void main() {

//    int sum = 17, count = 5;
//    double mean;

//    mean = (double) sum / count;
//    printf("Value of mean : %f\n", mean );
    // int x,y;
    // x=5;
    // y = x--; // x=4 , y =5
    // printf("x = %d, y = %d",x,y);

   int x = 13, y = 7;
	int z = (x - y) / 3;	
	printf("z = %d , ", z);
	printf("x = %d , y = %d , z = %d", x , y , y + z * 2);
}